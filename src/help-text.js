export default {
    flexDirection: "flex-direction sets the direction of the container's main axis and determines the direction in which items will be positioned.<br>flex-row means items will be positioned in the same direction as inline elements (typically left to right in English language sites).<br>flex-column means items will be positioned in the same direction as block elements (typically downwards in English language sites).",
    flexWrap: "flex-wrap determines whether flex items should wrap across multiple lines if they don't fit along the main axis.",
    justifyContent: "justify-content specifies how flex items should be distributed along the main axis.<br>flex-start and flex-end place items at the start and end, respectively, of the main axis.<br>center places items in the center.<br>space-around adds equal sized gaps before and after each item.<br>space-between places the first and last item of each line at the start and end of the line and distributes the remaining space evenly.<br>space-evenly is not part of the spec, but creates an equal size space between the line start and first item, between each item and between the last item and the line end.",
    alignItems: "align-items specifies how items are aligned about the main-axis (by translating items perpendicular to the main-axis).<br>In some cases, you'll only see a difference if the items are different sizes." 
}
